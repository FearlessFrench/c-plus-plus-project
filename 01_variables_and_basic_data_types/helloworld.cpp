#include <iostream>

int main() {

    // Variables
    int x; // declaration
    x = 5; // assignment

    int x = 5;
    int y = 6;

    std::cout << x << '\n';
    std::cout << y << '\n';
    std::cout << sum << '\n';

    // Data Types
    // integer (whole number)
    int age = 21;
    int year = 2023;
    int days = 7;

    // double (number including decimal)
    double price = 10.99;
    double gpa = 2.5;
    double temperature = 25.1;

    // single character (can't store more than one character)
    char grade = "A";
    char initial = "B";
    char dollarSign = "$";

    // boolean (true or false)
    bool student = true;
    bool power = false;
    bool forSale = true

    // string (objects that represents a sequence of text)
    std::string name = "みつは";
    std::string day = "Friday";
    std::string food = "Ramen";
    std::string town = "糸守";

    std::cout << "こんにちは " << name << '\n';
    std::cout << "あなたは " << age << "さいですか"

    std::cout << days; // 7
    std::cout << price; // 10.99
    std::cout << grade; // A
    std::cout << name; // みつは

    return 0;
}